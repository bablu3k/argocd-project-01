apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: nginx-data
  namespace: nginx

spec:
  accessModes:
    - ReadWriteMany

  volumeMode: Filesystem

  storageClassName: ""

  volumeName: nginx-nfs-pv

  resources:
    requests:
      storage: 1Gi
