apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pv.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:

  - name: nginx-html-source

    files:
      - nginx-html/index.html
